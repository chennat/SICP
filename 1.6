答案: 
a. new-if 仅是一个普通函数, 根据 Lisp 采用应用序求值规则, 两个函数将实参传入时便开始求值; 
即无论条件真假, then-clause 与 else-clause 都将求值. sqrt-iter 将一直递归下去.

b. sqrt-iter 的返回值要作为 new-if 的参数, 因此 sqrt-iter 的调用便不是尾递归. (???)
即无法进行递归的优化, 将最终突破解释器的最大递归深度, 从而造成错误.


知识补充:
1. 
(if <predicate> <consequence> <alternative>)
特殊形式 if: 解释器从求值其 <p> 判断条件开始; 若为真, 求值 <c> 作为返回值; 否则, 则求 <a> 并返回其值.

2. 
函数式编程语言的解释器实现一般对参数的求值顺序无特定的规则.

3. 
scheme 解释器的实现带有尾递归优化, 纯粹的尾递归不会造成解释器的栈溢出.


